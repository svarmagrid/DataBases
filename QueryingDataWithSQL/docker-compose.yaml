services:
  postgres:
    image: postgres:15
    container_name: QueryWithSQL
    environment:
      POSTGRES_DB: citizenship
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql

